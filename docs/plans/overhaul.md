Our website is a little bit too complex to use. So we're gonna solve this one niche problem, the interface is very simple. Drop your landing page URL and will give you analysis whether the solution, problem, fit works based on the description landing page URL.

frontend change:
For frontend, we should just remove the chat window as a whole and make a very professional workflow where user can pass in. Just very simple. Passing a URL for analysis and next to the URL there is an analysis button.


backend change:
For backend, we should perhaps remove the complete Google Form, Google Survey functionality in the prompt and optimize the prompt to be tailored a little bit more towards the product solution fit. This is for Capybara's system prompt.

All:
As a result, you can see that we will not have customers interact with the chat window anymore. But the backend database should still work the same or AI agent should still work the same. The only thing is we should narrow down the front end interaction surface to only evaluate the URL page. Once the user click on analysis, they don't need a sign in to click just on the analysis in the front landing page. Once users click on analysis, the entire page will be hitting a different UX page that has a professional dashboard-ish UX with our agents reasoning as some in some location. And we will ask the four typical mom test questions to the group of recruited clones. Actionable item section where, you know, basically based on the responses and statistics. What are the actions we can take to improve the overall you know a promising response rate to what basically some action items with potential impact.

