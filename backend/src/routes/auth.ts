/**
 * DEPRECATED: Auth routes removed as part of Supabase Auth migration.
 * Authentication is now handled by Supabase Auth with JWT verification in middleware/auth.ts
 * This file is kept as a placeholder for reference during the migration period.
 */

import { Router } from 'express'

const router = Router()

// All custom OAuth endpoints deprecated and removed
// Use Supabase Auth for authentication instead

export default router
